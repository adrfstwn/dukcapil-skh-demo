services:
  php:
    image: dunglas/frankenphp #php8.2
    container_name: dukcapil-laravel
    restart: always
    environment:
      - SERVER_NAME=dukcapil.adrfstwn.cloud
    ports:
      - "8081:80" #HTTP
      # - "443:443" #HTTPS
      - "8443:443/udp" #HTTP/3
    volumes:
      - ./:/app
      - caddy_data:/data
      - caddy_config:/config

volumes:
  caddy_data:
  caddy_config: